{
  "version": 2,
  "builds": [
    { "src": "api/telegram-webhook.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/telegram-webhook.js" }
  ],
  "installCommand": "npm install --legacy-peer-deps"
}
